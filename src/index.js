import React from 'react';
import ReactDOM from 'react-dom/client';
import ClappyApp from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ClappyApp />
  </React.StrictMode>
);
